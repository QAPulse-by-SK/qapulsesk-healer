# Runtime dependencies (LLM-agnostic core).
#
# Pick at least one LLM provider extra when you install:
#   pip install -e ".[anthropic]"    # Anthropic Claude
#   pip install -e ".[gemini]"       # Google Gemini (free tier available)
#
# This file exists for convenience. The canonical dependency list lives in
# pyproject.toml; CI installs via `pip install -e ".[dev]"`.

beautifulsoup4>=4.12.0
lxml>=5.0.0
pydantic>=2.0.0
